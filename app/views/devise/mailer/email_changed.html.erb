<p><%= t('.hello') %> <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t('.is_changing') %> <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p><%= t('.changed') %> <%= @resource.email %>.</p>
<% end %>
