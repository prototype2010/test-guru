<label><%= t('.tests') %></label>
<%= render 'shared/errors',
           resource: @test,
           local: true
%>
<table class="table table-hover">
  <thead>
    <tr>
      <th class="align-middle" scope="col"><%= t('.title') %></th>
      <th class="align-middle" scope="col"><%= t('.category') %></th>
      <th class="align-middle" scope="col"><%= t('.actions') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @tests  %>
  </tbody>
</table>
<%= link_to t('.create'), new_admin_test_path %>
