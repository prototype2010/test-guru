
<p><%= @answer.body %></p>

<%= button_to t('.back'), admin_question_path(@answer.question), method: :get %>
<%= button_to t('.edit'), edit_admin_answer_path(@answer) %>
<%= button_to t('.delete'), admin_answer_path(@answer.question), method: :delete %>
